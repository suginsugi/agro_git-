name: Security Vulnerability Scan

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: echo "Running Security Vulnerability Scan..."
