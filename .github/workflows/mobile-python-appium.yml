name: Mobile E2E Tests (Python/Appium)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: echo "Running Mobile E2E Tests (Python/Appium)..."
